## Robomaster EP Samples

These samples are created to show how to use the Robomaster EP using the Robomaster SDK.
To run these samples you need to import the environment using Anaconda with the `conda env create -f environment.yml` command.

I recommend using miniforge to manage your environments as it's easy to install and lightweight: <https://github.com/conda-forge/miniforge>
