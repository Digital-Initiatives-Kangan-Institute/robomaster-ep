# WRobomaster
This Python module is a wrapper around the Robomaster SDK modules to provide a clearer documentation.

## Usage
Copy the `wrobomaster` folder into the root of your Python project and import `wrobomaster` into your code.